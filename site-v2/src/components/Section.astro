---
interface Props {
  id: string;
  label: string;
}

const { id, label } = Astro.props;
---

<section id={id} class="reveal scroll-mt-24">
  <h2 class="font-mono font-semibold text-lg md:text-xl uppercase tracking-[0.14em] text-accent mb-8">
    {label}
  </h2>
  <slot />
</section>
